{% extends 'base.html.twig' %}

{% block title %}Edit Guestbook{% endblock %}

{% block body %}

<div class="card mt-3">
    <div class="card-header">
        <h2 class="card-title">Edit guestbook post</h2>
    </div>
    <div class="card-body">
        {{ include('guestbook/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="card-footer">
        <a class="btn btn-secondary" href="{{ path('app_guestbook_index') }}">Back to list</a>
        {{ include('guestbook/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
