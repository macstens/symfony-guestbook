{% extends 'base.html.twig' %}

{% block title %}New Guestbook{% endblock %}

{% block body %}
<div class="card mt-3">
    <div class="card-header">
        <h2 class="card-title">Create new guestbook post</h2>
    </div>
    <div class="card-body">
        {{ include('guestbook/_form.html.twig') }}
    </div>
    <div class="card-footer">
        <a class="btn btn-secondary" href="{{ path('app_guestbook_index') }}">Back to list</a>
    </div>
</div>
{% endblock %}
